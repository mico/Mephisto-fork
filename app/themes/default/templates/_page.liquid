<h2>{{ article.title }}</h2>
<div class="entry">{{ article.body }}</div>

<ul>
{% for page in section.pages %}
  {% if forloop.index > 1 %}
  <li>{{ page | link_to_page }}</li>
  {% endif %}
{% endfor %}
</ul>